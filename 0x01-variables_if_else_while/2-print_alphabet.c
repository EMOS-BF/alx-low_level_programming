#include<stdio.h>
/**
*main -prints the alphabet in lowercase
*followed by a new line
*return : 0
*/
int main(void)
{
  char ch;
  for(ch ='a'; ch <='2';ch++)
    {
      putchar(ch);
    }
  putchar('\n');
  return (0);
}
